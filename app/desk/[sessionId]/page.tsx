import Link from "next/link";import { Brand } from "@/components/brand";export default async function SavedResearch({params}:{params:Promise<{sessionId:string}>}){const{sessionId}=await params;return <main className="account-page"><nav className="nav shell"><Brand/><Link href="/desk">Back to desk</Link></nav><section className="account-empty"><h1>Saved research session</h1><p>Session <code>{sessionId}</code> is private. Open it from your authenticated research history so Fisk can verify your access token before loading evidence.</p></section></main>}
